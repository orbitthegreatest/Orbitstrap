<?xml version="1.0" encoding="utf-8"?>
<base:WpfUiWindow x:Class="Orbitstrap.UI.Elements.Dialogs.AddCustomThemeDialog" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:base="clr-namespace:Orbitstrap.UI.Elements.Base" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:local="clr-namespace:Orbitstrap.UI.Elements.Dialogs" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:resources="clr-namespace:Orbitstrap.Resources" xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml" xmlns:viewmodels="clr-namespace:Orbitstrap.UI.ViewModels.Dialogs" Title="Add Custom Theme" Width="480" MinHeight="0" ExtendsContentIntoTitleBar="True" ResizeMode="NoResize" SizeToContent="Height" WindowStartupLocation="CenterScreen" Background="{ui:ThemeResource ApplicationBackgroundBrush}">
  <Grid>
    <Grid>
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition Height="*" />
        <RowDefinition Height="Auto" />
      </Grid.RowDefinitions>
      <ui:TitleBar Title="{x:Static resources:Strings.CustomTheme_Add_Title}" Grid.Row="0" Grid.ColumnSpan="2" Padding="8" CanMaximize="False" KeyboardNavigation.TabNavigation="None" ShowMaximize="False" ShowMinimize="False" />
      <TabControl Name="Tabs" Grid.Row="1" Margin="16" SelectedIndex="{Binding Path=SelectedTab, Mode=TwoWay}">
        <TabItem Header="{x:Static resources:Strings.Common_CreateNew}">
          <Grid Row="1" Margin="16">
            <Grid.RowDefinitions>
              <RowDefinition Height="Auto" />
              <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="Auto" />
              <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid Row="0" ColumnSpan="2" Margin="0,0,0,12">
              <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
              </Grid.RowDefinitions>
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
              </Grid.ColumnDefinitions>
              <TextBlock Grid.Row="0" Grid.Column="0" MinWidth="100" VerticalAlignment="Center" Text="{x:Static resources:Strings.Common_Name}" />
              <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Path=Name, Mode=TwoWay}" />
              <TextBlock Grid.Row="1" Grid.Column="1" Foreground="{DynamicResource SystemFillColorCriticalBrush}" TextAlignment="Center" TextWrapping="Wrap" Text="{Binding Path=NameError, Mode=OneWay}" Visibility="{Binding Path=NameErrorVisibility, Mode=OneWay}" />
            </Grid>
            <TextBlock Grid.Row="1" Grid.Column="0" MinWidth="100" VerticalAlignment="Center" Text="{x:Static resources:Strings.Common_Template}" />
            <ComboBox Grid.Row="1" Grid.Column="1" ItemsSource="{Binding Path=Templates, Mode=OneTime}" Text="{Binding Path=Template, Mode=TwoWay}">
              <ItemsControl.ItemTemplate>
                <DataTemplate>
                  <TextBlock Text="{Binding Path=., Converter={StaticResource EnumNameConverter}}" >
  <TextBlock.Foreground><SolidColorBrush Color="{DynamicResource TextFillColorPrimary}"/></TextBlock.Foreground>
</TextBlock>
                </DataTemplate>
              </ItemsControl.ItemTemplate>
            </ComboBox>
          </Grid>
        </TabItem>
        <TabItem Header="{x:Static resources:Strings.Common_Import}">
          <Grid Margin="11">
            <Grid.RowDefinitions>
              <RowDefinition Height="*" />
              <RowDefinition Height="*" />
              <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" FontSize="14" TextAlignment="Center" TextWrapping="Wrap" Text="{Binding Path=FilePath}" Visibility="{Binding Path=FilePathVisibility}" Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
            <ui:Button Grid.Row="1" Margin="4" HorizontalAlignment="Stretch" Content="{x:Static resources:Strings.Common_ImportFromFile}" Icon="FolderOpen24" Click="OnImportButtonClicked" />
            <TextBlock Grid.Row="2" Foreground="{DynamicResource SystemFillColorCriticalBrush}" TextAlignment="Center" TextWrapping="Wrap" Text="{Binding Path=FileError}" Visibility="{Binding Path=FileErrorVisibility}" />
          </Grid>
        </TabItem>
      </TabControl>
      <Border Grid.Row="2" Margin="0,10,0,0" Padding="15" Background="{ui:ThemeResource SolidBackgroundFillColorSecondaryBrush}">
        <StackPanel HorizontalAlignment="Right" FlowDirection="LeftToRight" Orientation="Horizontal">
          <Button MinWidth="100" Content="{x:Static resources:Strings.Common_OK}" Click="OnOkButtonClicked" />
          <Button MinWidth="100" Margin="12,0,0,0" Content="{x:Static resources:Strings.Common_Cancel}" IsCancel="True" />
        </StackPanel>
      </Border>
    </Grid>
  </Grid>
</base:WpfUiWindow>