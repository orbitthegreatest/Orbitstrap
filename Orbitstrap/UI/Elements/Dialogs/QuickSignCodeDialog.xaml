<base:WpfUiWindow
    x:Class="Orbitstrap.UI.Elements.Dialogs.QuickSignCodeDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:base="clr-namespace:Orbitstrap.UI.Elements.Base"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Orbitstrap.UI.Elements.Dialogs"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="clr-namespace:Orbitstrap.UI.ViewModels"
    xmlns:control="clr-namespace:Orbitstrap.UI.Elements.Controls"
    xmlns:resources="clr-namespace:Orbitstrap.Resources"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="Quick Sign-In Dialog"
    Width="450"
    Height="345"
    MinWidth="450"
    MinHeight="345"
    Background="{ui:ThemeResource ApplicationBackgroundBrush}"
    ExtendsContentIntoTitleBar="True"
    ResizeMode="CanResize"
    WindowStartupLocation="CenterOwner"
    mc:Ignorable="d">

    <Grid Background="{DynamicResource ApplicationBackground}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ui:TitleBar
            Title="{x:Static resources:Strings.Menu_Dialog_QuickSignIn_Title}"
            Grid.Row="0"
            Padding="8"
            CanMaximize="False"
            ShowMaximize="False"
            Icon="pack://application:,,,/Orbitstrap.ico"
            KeyboardNavigation.TabNavigation="None"
            ShowMinimize="False" />

        <Grid Grid.Row="1" Margin="20,8,20,16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <StackPanel
                Grid.Row="0"
                Margin="0,0,0,16"
                Orientation="Horizontal">
                <ui:SymbolIcon
                    Margin="0,0,8,0"
                    FontSize="20"
                    Symbol="Link24" />
                <TextBlock
                    FontSize="18"
                    FontWeight="SemiBold"
                    Text="{x:Static resources:Strings.Menu_Dialog_QuickSignIn_Title}" />
            </StackPanel>

            <control:MarkdownTextBlock
                x:Name="InstructionsText"
                Grid.Row="1"
                Margin="0,0,0,12"
                FontSize="14"
                Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                MarkdownText="Copy this code and paste it in the [Quick Sign-In Page](https://www.roblox.com/crossdevicelogin/ConfirmCode)."
                TextWrapping="Wrap"
                Visibility="Visible" />

            <Border
                x:Name="CodeBox"
                Grid.Row="2"
                Margin="0,0,0,16"
                Padding="16,12"
                Background="{DynamicResource ControlFillColorSecondaryBrush}"
                BorderBrush="{DynamicResource ControlElevationBorderBrush}"
                BorderThickness="1"
                CornerRadius="6"
                Visibility="Collapsed">
                <TextBox
                    x:Name="CodeTextBox"
                    HorizontalAlignment="Stretch"
                    VerticalContentAlignment="Center"
                    Background="Transparent"
                    BorderThickness="0"
                    FontFamily="Consolas"
                    FontSize="20"
                    FontWeight="Bold"
                    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                    IsReadOnly="True"
                    TextAlignment="Center"
                    TextWrapping="NoWrap" />
            </Border>

            <Border 
                Grid.Row="3"
                Background="{DynamicResource SystemFillColorAttentionBackgroundBrush}"
                BorderBrush="{DynamicResource SystemFillColorAttentionBrush}"
                BorderThickness="1"
                CornerRadius="6"
                Padding="12,10">
                <StackPanel Orientation="Horizontal">
                    <ui:SymbolIcon 
                        Symbol="Info24" 
                        FontSize="16"
                        Margin="0,0,8,0"
                        Foreground="{DynamicResource SystemFillColorAttentionBrush}"/>
                    <TextBlock 
                        x:Name="StatusText"
                        Text="Waiting for Quick Sign-In...&#x0a;The app will close this window when sign-in completes." 
                        TextWrapping="Wrap"
                        FontSize="13"
                        Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                        VerticalAlignment="Center"/>
                </StackPanel>
            </Border>

            <Rectangle Grid.Row="4" Fill="Transparent" />
        </Grid>

        <Border
            Grid.Row="2"
            Background="{DynamicResource ControlFillColorDefaultBrush}"
            BorderBrush="{DynamicResource ControlElevationBorderBrush}"
            BorderThickness="1,1,0,0">
            <Grid Margin="20,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <Rectangle Grid.Column="0" Fill="Transparent" />

                <ui:Button
                    Grid.Column="1"
                    MinWidth="100"
                    Margin="0,0,8,0"
                    HorizontalAlignment="Right"
                    Appearance="Secondary"
                    Click="Copy_Click"
                    Content="Copy Code"/>

                <ui:Button
                    Grid.Column="2"
                    MinWidth="100"
                    HorizontalAlignment="Right"
                    Click="Close_Click"
                    Content="Close"/>
            </Grid>
        </Border>
    </Grid>
</base:WpfUiWindow>