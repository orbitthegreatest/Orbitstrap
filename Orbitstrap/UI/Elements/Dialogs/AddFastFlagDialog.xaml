<?xml version="1.0" encoding="utf-8"?>
<base:WpfUiWindow x:Class="Orbitstrap.UI.Elements.Dialogs.AddFastFlagDialog" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml" xmlns:local="clr-namespace:Orbitstrap.UI.Elements.Dialogs" xmlns:base="clr-namespace:Orbitstrap.UI.Elements.Base" xmlns:resources="clr-namespace:Orbitstrap.Resources" xmlns:converters="clr-namespace:Orbitstrap.UI.Converters" Title="{x:Static resources:Strings.Dialog_AddFastFlag_Title}" MinHeight="0" Width="480" SizeToContent="Height" ResizeMode="NoResize" ExtendsContentIntoTitleBar="True" WindowStartupLocation="CenterScreen" Background="{ui:ThemeResource ApplicationBackgroundBrush}">
  <FrameworkElement.Resources>
    <ResourceDictionary>
      <converters:RangeConverter x:Key="ValidationConverter" From="0" />
    </ResourceDictionary>
  </FrameworkElement.Resources>
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>
    <ui:TitleBar Grid.Row="0" Grid.ColumnSpan="2" Padding="8" Title="{x:Static resources:Strings.Dialog_AddFastFlag_Title}" ShowMinimize="False" ShowMaximize="False" CanMaximize="False" KeyboardNavigation.TabNavigation="None" />
    <TabControl Name="Tabs" Grid.Row="1" Margin="16">
      <TabItem Header="{x:Static resources:Strings.Common_AddSingle}">
        <Grid Row="1" Margin="16">
          <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
          </Grid.RowDefinitions>
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
          </Grid.ColumnDefinitions>
          <TextBlock Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" MinWidth="100" Text="{x:Static resources:Strings.Common_Name}" Margin="0,0,0,12" />
          <TextBox Name="FlagNameTextBox" Grid.Row="0" Grid.Column="1" Margin="0,0,0,12" />
          <TextBlock Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" MinWidth="100" Text="{x:Static resources:Strings.Common_Value}" />
          <TextBox Name="FlagValueTextBox" Grid.Row="1" Grid.Column="1" />
        </Grid>
      </TabItem>
      <TabItem Header="{x:Static resources:Strings.Common_ImportJson}">
        <Grid Margin="11">
          <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
          </Grid.RowDefinitions>
          <TextBox Name="JsonTextBox" Grid.Row="0" Margin="5" AcceptsTab="True" AcceptsReturn="True" MinHeight="80" MaxHeight="480" />
          <TextBlock Grid.Row="0" IsHitTestVisible="False" Margin="18,14,0,0" Foreground="#FFA9A9A9" Text="{}{&#xA;    &quot;FFlagDebugDisplayFPS&quot;: &quot;True&quot;&#xA;}">
            <FrameworkElement.Style>
              <Style TargetType="{x:Type TextBlock}">
                <Setter Property="Visibility" Value="Collapsed" />
                <Style.Triggers>
                  <DataTrigger Value="" Binding="{Binding Text, ElementName=JsonTextBox}">
                    <Setter Property="Visibility" Value="Visible" />
                  </DataTrigger>
                </Style.Triggers>
              </Style>
            </FrameworkElement.Style>
          </TextBlock>
          <ui:Button Grid.Row="1" Margin="4" Icon="FolderOpen24" Content="{x:Static resources:Strings.Common_ImportFromFile}" HorizontalAlignment="Stretch" Click="ImportButton_Click" />
        </Grid>
      </TabItem>
    </TabControl>
    <Border Grid.Row="2" Margin="0,10,0,0" Padding="15" Background="{ui:ThemeResource SolidBackgroundFillColorSecondaryBrush}">
      <StackPanel Orientation="Horizontal" FlowDirection="LeftToRight" HorizontalAlignment="Right">
        <Button MinWidth="100" Content="{x:Static resources:Strings.Common_OK}" Click="OKButton_Click">
          <FrameworkElement.Style>
            <Style BasedOn="{StaticResource {x:Type Button}}" TargetType="{x:Type Button}">
              <Style.Triggers>
                <MultiDataTrigger>
                  <MultiDataTrigger.Conditions>
                    <Condition Value="0" Binding="{Binding ElementName=Tabs, Path=SelectedIndex}" />
                    <Condition Value="True" Binding="{Binding ElementName=FlagNameTextBox, Path=Text.Length, Converter={StaticResource ValidationConverter}}" />
                  </MultiDataTrigger.Conditions>
                  <Setter Property="IsEnabled" Value="True" />
                </MultiDataTrigger>
                <MultiDataTrigger>
                  <MultiDataTrigger.Conditions>
                    <Condition Value="1" Binding="{Binding ElementName=Tabs, Path=SelectedIndex}" />
                    <Condition Value="True" Binding="{Binding ElementName=JsonTextBox, Path=Text.Length, Converter={StaticResource ValidationConverter}}" />
                  </MultiDataTrigger.Conditions>
                  <Setter Property="IsEnabled" Value="True" />
                </MultiDataTrigger>
              </Style.Triggers>
              <Setter Property="IsEnabled" Value="False" />
            </Style>
          </FrameworkElement.Style>
        </Button>
        <Button MinWidth="100" Margin="12,0,0,0" Content="{x:Static resources:Strings.Common_Cancel}" IsCancel="True" />
      </StackPanel>
    </Border>
  </Grid>
</base:WpfUiWindow>