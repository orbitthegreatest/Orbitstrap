<base:WpfUiWindow x:Class="Orbitstrap.UI.Elements.AccountManager.MainWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:pages="clr-namespace:Orbitstrap.UI.Elements.AccountManager.Pages"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:base="clr-namespace:Orbitstrap.UI.Elements.Base"
             xmlns:resources="clr-namespace:Orbitstrap.Resources"
             mc:Ignorable="d"
             Title="Account Manager"
             Background="{ui:ThemeResource ApplicationBackgroundBrush}"
             Width="1000"
             Height="675"
             ExtendsContentIntoTitleBar="True"
             WindowBackdropType="Mica"
             WindowStartupLocation="CenterScreen">

    <Grid Background="{DynamicResource ApplicationBackground}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ui:TitleBar Padding="8" x:Name="RootTitleBar" Grid.Row="0" ForceShutdown="False" MinimizeToTray="False" UseSnapLayout="True" Title="Account Manager" Icon="pack://application:,,,/Orbitstrap.ico" />

        <Grid x:Name="RootGrid" Grid.Row="1" Margin="12,12,0,0" Visibility="Visible">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <ui:NavigationStore x:Name="RootNavigation" Grid.Row="1" Grid.Column="0" Margin="0,0,12,0" Frame="{Binding ElementName=RootFrame}" SelectedPageIndex="0">
                <ui:NavigationStore.Items>
                    <ui:NavigationItem Content="Accounts" PageType="{x:Type pages:AccountsPage}" Icon="PersonAccounts24" Tag="accounts" Margin="0,0,0,12" FontWeight="Medium" />
                    <ui:NavigationItem Content="Friends" PageType="{x:Type pages:FriendsPage}" Icon="People24" Tag="friends" Margin="0,0,0,12" FontWeight="Medium" x:Name="friends"/>
                    <ui:NavigationItem Content="Games" PageType="{x:Type pages:GamesPage}" Icon="Games24" Tag="games" Margin="0,0,0,12" FontWeight="Medium" x:Name="games"/>
                </ui:NavigationStore.Items>
            </ui:NavigationStore>

            <Frame x:Name="RootFrame" Grid.Row="0" Grid.RowSpan="2" Grid.Column="1" />
        </Grid>

        <Grid x:Name="LoadingOverlay" Grid.Row="1" Grid.RowSpan="2" Visibility="Collapsed" Background="#A0000000" Panel.ZIndex="9999" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">

            <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
                <Border Background="{DynamicResource NormalDarkAndLightBackground}" CornerRadius="16" Padding="32" Effect="{DynamicResource ControlShadow}">
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Orientation="Vertical">

                        <TextBlock x:Name="LoadingOverlayText" Text="Loading..." FontSize="22" FontWeight="SemiBold" Margin="0,0,0,20" HorizontalAlignment="Center" Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
                        <ui:ProgressRing IsIndeterminate="True" Width="60" Height="60" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,10,0,0" />

                    </StackPanel>
                </Border>
            </Grid>
        </Grid>
    </Grid>
</base:WpfUiWindow>
