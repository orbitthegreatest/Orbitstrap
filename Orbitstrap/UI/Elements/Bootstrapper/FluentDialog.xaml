<?xml version="1.0" encoding="utf-8"?>
<base:WpfUiWindow x:Class="Orbitstrap.UI.Elements.Bootstrapper.FluentDialog" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:base="clr-namespace:Orbitstrap.UI.Elements.Base" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:resources="clr-namespace:Orbitstrap.Resources" xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml" xmlns:vms="clr-namespace:Orbitstrap.UI.ViewModels.Bootstrapper" Closing="UiWindow_Closing" Width="500" Height="280" MinHeight="0" ExtendsContentIntoTitleBar="True" ResizeMode="NoResize" WindowStartupLocation="CenterScreen" WindowStyle="None" xmlns:bootstrapper="clr-namespace:Orbitstrap.UI.Elements.Bootstrapper" Background="{ui:ThemeResource ApplicationBackgroundBrush}" WindowBackdropType="{Binding Path=WindowBackdropType, Mode=OneTime}">
  <Window.TaskbarItemInfo>
    <TaskbarItemInfo ProgressState="{Binding Path=TaskbarProgressState}" ProgressValue="{Binding Path=TaskbarProgressValue}" />
  </Window.TaskbarItemInfo>
  <Grid Background="{Binding Path=BackgroundColourBrush, Mode=OneTime}">
    <TextBlock Margin="5" VerticalAlignment="Top" HorizontalAlignment="Right" FontSize="10" FontFamily="{StaticResource Rubik}" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Text="{Binding VersionText}" />
    <TextBlock Margin="5" VerticalAlignment="Top" HorizontalAlignment="Left" FontSize="10" FontFamily="{StaticResource Rubik}" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Text="{Binding ChannelText}" />
    <ui:TitleBar VerticalAlignment="Top" CanMaximize="False" ShowClose="False" ShowMaximize="False" ShowMinimize="False" />
    <Grid Margin="32,16">
      <Grid.RowDefinitions>
        <RowDefinition Height="*" />
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
      </Grid.RowDefinitions>
      <Image Grid.Row="0" Width="80" Height="80" Margin="0,30,0,0" HorizontalAlignment="Center" VerticalAlignment="Top" RenderOptions.BitmapScalingMode="HighQuality" Source="{Binding Icon, Mode=OneWay}" />
      <TextBlock Grid.Row="1" Margin="0,0,0,8" HorizontalAlignment="Center" TextAlignment="Center" FontSize="18" Foreground="{DynamicResource TextFillColorPrimaryBrush}" TextWrapping="Wrap" Text="{Binding Message, Mode=OneWay}" />
      <ProgressBar Grid.Row="2" Margin="0,0,0,16" IsIndeterminate="{Binding ProgressIndeterminate, Mode=OneWay}" Maximum="{Binding ProgressMaximum, Mode=OneWay}" Value="{Binding ProgressValue, Mode=OneWay}" />
      <Button Grid.Row="3" Width="120" Padding="4" HorizontalAlignment="Center" Content="{x:Static resources:Strings.Common_Cancel}" FontSize="14" Command="{Binding CancelInstallCommand}" IsEnabled="{Binding CancelEnabled, Mode=OneWay}" />
    </Grid>
  </Grid>
</base:WpfUiWindow>