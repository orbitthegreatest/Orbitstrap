<?xml version="1.0" encoding="utf-8"?>
<base:WpfUiWindow x:Class="Orbitstrap.UI.Elements.ContextMenu.ServerHistory" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:local="clr-namespace:Orbitstrap.UI.Elements.ContextMenu" xmlns:base="clr-namespace:Orbitstrap.UI.Elements.Base" xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml" xmlns:models="clr-namespace:Orbitstrap.UI.ViewModels.ContextMenu" xmlns:resources="clr-namespace:Orbitstrap.Resources" xmlns:enums="clr-namespace:Orbitstrap.Enums" Title="{x:Static resources:Strings.ContextMenu_GameHistory_Title}" MinWidth="420" MinHeight="420" Width="580" Height="420" ExtendsContentIntoTitleBar="True" WindowStartupLocation="CenterScreen" Background="{ui:ThemeResource ApplicationBackgroundBrush}">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>
    <ui:TitleBar Name="RootTitleBar" Grid.Row="0" Grid.ColumnSpan="2" Padding="8" Title="{x:Static resources:Strings.ContextMenu_GameHistory_Title}" ShowMinimize="False" ShowMaximize="False" CanMaximize="False" KeyboardNavigation.TabNavigation="None" Icon="pack://application:,,,/Orbitstrap.ico" />
    <TextBlock Grid.Row="1" Margin="16,8,16,0" Text="{x:Static resources:Strings.ContextMenu_GameHistory_Description}" TextWrapping="Wrap" />
    <TextBlock Grid.Row="2" Margin="16,8,16,0" TextWrapping="Wrap" Text="{Binding Error, Mode=OneWay}">
      <FrameworkElement.Style>
        <Style TargetType="{x:Type TextBlock}">
          <Style.Triggers>
            <DataTrigger Value="{x:Static enums:GenericTriState.Failed}" Binding="{Binding LoadState, Mode=OneWay}">
              <Setter Property="Visibility" Value="Visible" />
            </DataTrigger>
          </Style.Triggers>
          <Setter Property="Visibility" Value="Collapsed" />
        </Style>
      </FrameworkElement.Style>
    </TextBlock>
    <Border Grid.Row="2">
      <FrameworkElement.Style>
        <Style TargetType="{x:Type Border}">
          <Style.Triggers>
            <DataTrigger Value="{x:Static enums:GenericTriState.Unknown}" Binding="{Binding LoadState, Mode=OneWay}">
              <Setter Property="Visibility" Value="Visible" />
            </DataTrigger>
          </Style.Triggers>
          <Setter Property="Visibility" Value="Collapsed" />
        </Style>
      </FrameworkElement.Style>
      <ui:ProgressRing Grid.Row="1" IsIndeterminate="True" />
    </Border>
    <ListView Grid.Row="2" Margin="8" ItemsSource="{Binding GameHistory, Mode=OneWay}">
      <FrameworkElement.Style>
        <Style BasedOn="{StaticResource {x:Type ListView}}" TargetType="{x:Type ListView}">
          <Style.Triggers>
            <DataTrigger Value="{x:Static enums:GenericTriState.Successful}" Binding="{Binding LoadState, Mode=OneWay}">
              <Setter Property="Visibility" Value="Visible" />
            </DataTrigger>
          </Style.Triggers>
          <Setter Property="Visibility" Value="Collapsed" />
        </Style>
      </FrameworkElement.Style>
      <ItemsControl.ItemContainerStyle>
        <Style BasedOn="{StaticResource {x:Type ListViewItem}}" TargetType="{x:Type ListViewItem}">
          <Setter Property="Focusable" Value="False" />
        </Style>
      </ItemsControl.ItemContainerStyle>
      <ItemsControl.ItemTemplate>
        <DataTemplate>
          <ui:Card Padding="0">
            <Grid VerticalAlignment="Center">
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
              </Grid.ColumnDefinitions>
              <Border Grid.Column="0" Width="107" Height="107" CornerRadius="4">
                <Border.Background>
                  <ImageBrush ImageSource="{Binding UniverseDetails.Thumbnail.ImageUrl, IsAsync=True}" />
                </Border.Background>
              </Border>
              <StackPanel Grid.Column="1" Margin="16,12,12,12" VerticalAlignment="Center">
                <TextBlock FontSize="18" FontWeight="Medium" Text="{Binding UniverseDetails.Data.Name}" >
  <TextBlock.Foreground><SolidColorBrush Color="{DynamicResource TextFillColorPrimary}"/></TextBlock.Foreground>
</TextBlock>
                <TextBlock Foreground="{DynamicResource TextFillColorSecondaryBrush}" Text="{Binding GameHistoryDescription}" />
                <ui:Button Margin="0,8,0,0" Content="{x:Static resources:Strings.ContextMenu_GameHistory_Rejoin}" Appearance="Success" Foreground="#FFFFFFFF" Icon="Play24" IconFilled="True" Command="{Binding RejoinServerCommand}" />
              </StackPanel>
            </Grid>
          </ui:Card>
        </DataTemplate>
      </ItemsControl.ItemTemplate>
    </ListView>
  </Grid>
</base:WpfUiWindow>