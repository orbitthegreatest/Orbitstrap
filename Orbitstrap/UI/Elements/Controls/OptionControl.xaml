<?xml version="1.0" encoding="utf-8"?>
<UserControl x:Class="Orbitstrap.UI.Elements.Controls.OptionControl" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml" xmlns:models="clr-namespace:Orbitstrap.UI.ViewModels" xmlns:resources="clr-namespace:Orbitstrap.Resources" xmlns:local="clr-namespace:Orbitstrap.UI.Elements.Controls" x:Name="Control">
  <ui:CardControl Margin="0,8,0,0" Content="{Binding InnerContent, ElementName=Control}">
    <ui:CardControl.Header>
      <StackPanel>
        <Grid>
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
          </Grid.ColumnDefinitions>
          <TextBlock x:Name="PART_Header" Grid.Column="0" FontSize="14">
            <TextBlock.Style>
              <Style TargetType="{x:Type TextBlock}">
                <Setter Property="Foreground">
                  <Setter.Value>
                    <SolidColorBrush Color="{DynamicResource TextFillColorPrimary}" />
                  </Setter.Value>
                </Setter>
              </Style>
            </TextBlock.Style>
          </TextBlock>
          <TextBlock x:Name="PART_HelpLinkContainer" Grid.Column="1" Margin="4,0,0,0" Visibility="Collapsed">
            <Hyperlink TextDecorations="None" ToolTip="{x:Static resources:Strings.Menu_MoreInfo}" Command="models:GlobalViewModel.OpenWebpageCommand">
              <Hyperlink.CommandParameter>
                <Binding ElementName="PART_HelpLinkContainer" Path="Tag" />
              </Hyperlink.CommandParameter>
              <ui:SymbolIcon Symbol="QuestionCircle48" Margin="0,1,0,0">
                <FrameworkElement.Style>
                  <Style BasedOn="{StaticResource {x:Type ui:SymbolIcon}}" TargetType="{x:Type ui:SymbolIcon}">
                    <Style.Triggers>
                      <Trigger Property="UIElement.IsMouseOver" Value="True">
                        <Setter Property="Foreground">
                          <Setter.Value>
                            <SolidColorBrush Color="{DynamicResource SystemAccentColorTertiary}" />
                          </Setter.Value>
                        </Setter>
                      </Trigger>
                    </Style.Triggers>
                    <Setter Property="Foreground">
                      <Setter.Value>
                        <SolidColorBrush Color="{DynamicResource SystemAccentColorSecondary}" />
                      </Setter.Value>
                    </Setter>
                  </Style>
                </FrameworkElement.Style>
              </ui:SymbolIcon>
            </Hyperlink>
          </TextBlock>
        </Grid>
        <local:MarkdownTextBlock x:Name="PART_Description" Margin="0,2,0,0" FontSize="12" TextWrapping="Wrap" Visibility="Collapsed">
          <local:MarkdownTextBlock.Style>
            <Style TargetType="{x:Type local:MarkdownTextBlock}">
              <Setter Property="Foreground">
                <Setter.Value>
                  <SolidColorBrush Color="{DynamicResource TextFillColorTertiary}" />
                </Setter.Value>
              </Setter>
            </Style>
          </local:MarkdownTextBlock.Style>
        </local:MarkdownTextBlock>
      </StackPanel>
    </ui:CardControl.Header>
  </ui:CardControl>
</UserControl>
